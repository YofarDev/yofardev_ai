// import 'dart:convert';
// import 'dart:html' as html;

// import 'package:flutter/services.dart';
// import 'package:just_audio/just_audio.dart';

// import '../services/google_tts_service.dart';

void testAmplitudesWeb() async {
  // var a = await GoogleTtsService.texttospeech(
  //     "This is a test my friend", GoogleTtsService.malevoice);
  // var b = a.bodyBytes;
  // final Uint8List uint8List = b.buffer.asUint8List();
  // final html.Blob blob = html.Blob([uint8List]);
  // final String url = html.Url.createObjectUrlFromBlob(blob);
  // print(url);
  // final html.AudioElement audioElement = html.AudioElement();
  // audioElement.src = url;
  // AudioPlayer player = AudioPlayer();
  // await player.setAudioSource(AudioSource.uri(Uri.parse(url)));
  // await player.play();
  //  //     audioElement.play();
  // final List<int>? amplitudes = await getAmplitudes(url);
  // print(amplitudes);
  // print(amplitudes?.length);
}
